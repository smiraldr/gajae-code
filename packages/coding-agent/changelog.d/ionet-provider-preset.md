### Added

- `gjc setup provider --preset ionet` (aliases: `io-net`, `io-intelligence`): OpenAI-compatible custom provider for IO Intelligence by io.net (`IONET_API_KEY`, live `/models` discovery, `org/name` model ids).
